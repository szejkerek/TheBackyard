/// <summary>
/// Night management script for handling tasks and logic during the night phase.
/// </summary>
public class NightManagement : MonoBehaviour
{
    /// <summary>
    /// Called before the first frame update. Used for initialization or tasks at the start of the night.
    /// </summary>
    void Start()
    {
        // Initialization or tasks to perform at the start of the night
    }

    /// <summary>
    /// Called once per frame. Contains night-time logic or tasks to perform each frame during the night.
    /// </summary>
    void Update()
    {
        // Nighttime logic or tasks to perform each frame during the night
    }
}
